<h1>Listing Users</h1>
<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<table class="type07">
  <thead>
    <tr>
      <th><font size=5>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><font size=4><%= user.name %></td>
        <td><%= link_to 'Show', user, class:"redBtn"%></td>
        <td><%= link_to 'Edit', edit_user_path(user), class:"redBtn" %></td>
        <td><%= link_to 'Destroy', user, class:"redBtn", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

